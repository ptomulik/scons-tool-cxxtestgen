[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
scons-tool-util = ">=0.1.8"

[dev-packages]
scons-select = { path = "./deps/scons-select" }
twine = "*"

[requires]
python = "2.7"
